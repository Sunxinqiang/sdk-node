name: auto_pr

on:
  push:
    branches:
      - trunk
  workflow_dispatch:

jobs:
  create_pr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: gh pr create --base main --title auto_pr --body $(git log -1 --pretty=format:'%s')
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
